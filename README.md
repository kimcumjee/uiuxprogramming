# 하루한칸

하루 10분, 오늘 공부한 것을 한 칸씩 채워 학습 습관이 끊기지 않게 하는 서비스의 소개 페이지입니다.

- 주요 사용자: 혼자 공부하지만 며칠 지나면 기록이 끊기는 대학생과 자격증 준비생
- 해결하려는 문제: 계획은 세우지만 기록이 남지 않아 꾸준히 하고 있는지 알 수 없다
- 핵심 행동: 필요한 영역으로 이동하고 이메일로 출시 소식을 신청한다

## 프로젝트 구조

```
uiuxprogramming/
├─ index.html
├─ style.css
├─ script.js
├─ README.md
├─ docs/
│  ├─ 1주차-실행화면.png
│  └─ 2주차-실행화면.png
└─ assets/
   └─ images/
      └─ service.jpg
```

## 주차별 작업

| 주차 | 내용 |
| --- | --- |
| 1주차 | 기본 HTML 문서 구조를 작성하고 제목과 문단을 내 서비스 내용으로 작성 |
| 2주차 | header·main·section·footer 시맨틱 구조, 목록, 이미지, 최소 입력 영역, 내부 이동 링크로 확장 |

## 2주차에 바꾼 구조와 변경 이유

### 1. h1과 p를 header 안으로 옮김

1주차에는 `h1`과 `p`가 `body` 바로 아래에 있었습니다. 2주차에 콘텐츠가 늘어나면서
"페이지 전체를 대표하는 머리말"과 "본문"을 구분할 필요가 생겨 `header`로 묶었습니다.
이후 `nav`도 같은 머리말 영역에 들어가므로, 서비스 이름·소개·이동 메뉴가 하나의
묶음이 되는 편이 포함 관계가 분명합니다.

### 2. 한 줄 소개 문장을 다시 씀

1주차 문장은 "~한 학습 습관 트래커입니다"로 서비스의 **정체**만 말했습니다.
소개 페이지에서는 사용자가 얻는 것이 먼저 보여야 한다고 판단해,
"하루 10분, 오늘 공부한 것을 한 칸씩 채워 학습 습관이 끊기지 않게 합니다"로
해결하는 문제와 제공하는 가치가 함께 드러나게 고쳤습니다.

### 3. 주요 기능은 ul, 이용 방법은 ol

처음에는 둘 다 순서 있는 목록으로 쓸까 고민했습니다. 하지만 주요 기능 3개는
순서를 바꿔도 의미가 그대로이고, 이용 방법 3단계는 과목 등록 → 기록 → 확인 순서를
지켜야 의미가 성립합니다. 그래서 기능은 `ul`, 이용 방법은 `ol`로 나눴습니다.
목록 종류 자체가 "순서가 의미를 갖는가"를 알려 주는 정보라고 보았습니다.

### 4. 기능 목록의 li 안에 h3을 넣음

기능 이름을 `strong`으로 쓸 수도 있었지만, 기능 이름은 굵은 글씨가 아니라
"주요 기능(h2)에 속한 세부 주제"입니다. 글자 크기가 아니라 정보 계층에 따라
선택해야 하므로 `h3`을 썼습니다.

### 5. 이용 방법의 li에는 제목을 넣지 않음

기능과 달리 이용 방법의 각 단계는 한 문장이면 충분해서, `h3`으로 소제목을 만들면
계층만 늘고 읽는 데는 도움이 되지 않는다고 보아 `li` 안에 문장만 두었습니다.

### 6. 입력 영역의 제목과 버튼 문구를 서비스에 맞게 바꿈

서비스가 아직 출시 전이라 핵심 행동이 "가입"이 아니라 "출시 소식 신청"입니다.
버튼을 "제출"이 아니라 "출시 소식 받기"로 써서 눌렀을 때 기대되는 결과가
문구에 드러나게 했습니다.

### 7. 이미지를 서비스 소개 문단 뒤에 배치

이미지를 `header` 바로 아래에 두면 시선은 끌지만, 무엇을 보는 화면인지 모르는
상태에서 보게 됩니다. 글로 서비스를 설명한 뒤 그 설명을 확인시키는 순서가
맞다고 판단해 `section#about`의 설명 문단 다음에 두었습니다.

### 8. alt에 화면의 의미를 씀

"학습 기록 화면" 같은 요약 대신, 이미지가 이 위치에서 전달하려는 내용인
"21칸 중 18칸이 채워져 있고 오늘 칸은 비어 기록을 기다린다"를 그대로 적었습니다.
이미지를 못 보는 사용자도 "꾸준히 채워지고 있다"는 메시지를 받게 하려는 의도입니다.

## AI가 만든 같은 페이지와 비교

같은 기획을 주고 AI에게 만들게 한 HTML과 직접 구현한 결과를 비교했습니다.

### 정보 계층

AI 버전은 각 기능을 `article` + `h3`으로 만들고 `ul` 없이 나열했습니다.
`article`은 그 자체로 독립해서 배포될 수 있는 콘텐츠에 쓰는 태그라,
"주요 기능 3개"처럼 함께 있어야 의미가 생기는 항목에는 과한 선택입니다.
목록으로 묶이지 않아 "이것이 3개짜리 한 묶음"이라는 정보도 사라졌습니다.
직접 구현한 버전은 하나의 `ul` 안에 `li` 3개로 두어 묶음이라는 사실이 구조에 남습니다.

### 시맨틱 구조

AI 버전은 `section` 사이마다 `hr`을 넣어 구분했습니다. 화면에서는 선이 그어져
구분되어 보이지만, `hr`은 주제 전환을 뜻하는 요소이고 이미 `section`이 같은 일을
하고 있어 의미가 중복됩니다. 보이는 결과를 만들려고 태그를 쓴 경우라
직접 구현한 버전에서는 뺐습니다.

또 AI 버전은 UX 계획 주석을 `body` 밖(`head`와 `body` 사이)에 두었습니다.
브라우저는 그래도 보여 주지만 과제 조건은 `body` 첫 줄이고, 문서 구조상으로도
본문 계획은 본문 안에 있는 편이 맞습니다.

### 접근성

- AI 버전의 `nav`에는 `aria-label`이 없었습니다. 페이지에 `nav`가 하나뿐이면
  큰 문제는 아니지만, 스크린 리더가 여러 내비게이션을 구분해 읽어 주려면
  이름이 필요하므로 `aria-label="주요 메뉴"`를 붙였습니다.
- AI 버전은 입력 항목을 이름·이메일·목표·의견 4개로 늘렸습니다. 요구된 것은
  최소 입력 영역이고, 소식 신청에 실제로 필요한 값은 이메일 하나뿐입니다.
  입력 부담을 늘리는 만큼 중간에 그만두는 사용자가 생기므로 한 칸으로 줄였습니다.
- `label`과 `input`의 연결은 두 버전 모두 `for`와 `id`로 되어 있었습니다.
  라벨을 눌렀을 때 입력칸이 선택되는지 브라우저에서 직접 확인했습니다.

### 정리

AI 버전은 태그 종류가 더 많았지만, 태그를 고른 이유가 "화면에서 그렇게 보이니까"인
경우가 있었습니다. 이번 주 목표가 역할·순서·포함 관계를 구조로 표현하는 것이었으므로,
태그 수를 늘리기보다 각 태그가 왜 그 자리에 있는지 설명할 수 있는 쪽을 택했습니다.
